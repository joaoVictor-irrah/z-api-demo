export class Chat {
  id: string;
  name: string;
  phone: string;
  lid: string;

  constructor(id: string, name: string, phone: string, lid: string) {
    this.id = id;
    this.name = name;
    this.phone = phone;
    this.lid = lid;
  }
}
